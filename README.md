# AWOL-leave-management
HR App that monitors and controls leave applications in an organisation. The system was built with Python and uses a Flet UI. It is integrated with FastAPI from SQLite3 database (backend) to the Flet UI (front end)
